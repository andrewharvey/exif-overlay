# exif-overlay

Annotate a directory of images with selected EXIF data

## Requirements

* bash >= 4
* convert (ImageMagick)
* exiftool

## Usage

    exif-overlay <input_directory> <output_directory>
